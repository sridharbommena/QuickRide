spring:
  application:
    name: TRIP-SERVICE
  datasource:
    url: jdbc:h2:mem:testdb
    username: user
    password: user123

  cloud:
    openfeign:
      okhttp:
        enabled: true


server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

